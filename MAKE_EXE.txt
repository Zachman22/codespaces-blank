═══════════════════════════════════════════════════════════════
              🎯 MAKE HYBRIDIDE.EXE - SIMPLE GUIDE
═══════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────┐
│                    EASIEST METHOD                           │
└─────────────────────────────────────────────────────────────┘

1. Find BUILD.bat in the hybrid-ide folder
2. Double-click it
3. Wait for build to complete
4. Run HybridIDE.exe

Done! 🎉

═══════════════════════════════════════════════════════════════
                     STEP-BY-STEP GUIDE
═══════════════════════════════════════════════════════════════

PREREQUISITES:
✓ MinGW installed (g++ compiler)
✓ Node.js installed (for React build)
✓ Windows 11

═══════════════════════════════════════════════════════════════

STEP 1: Open PowerShell or Command Prompt
─────────────────────────────────────────

Press Win+R, type "powershell", press Enter

═══════════════════════════════════════════════════════════════

STEP 2: Navigate to project folder
─────────────────────────────────────────

cd C:\path\to\hybrid-ide

═══════════════════════════════════════════════════════════════

STEP 3: Build the React frontend
─────────────────────────────────────────

cd frontend
npm install
npm run build
cd ..

Expected output:
  ✓ React app bundled to frontend/dist/
  ✓ index.html created
  ✓ JavaScript/CSS bundles created

═══════════════════════════════════════════════════════════════

STEP 4: Compile C++ backend
─────────────────────────────────────────

Run ONE of these:

Option A - Use build script:
  .\build.ps1

Option B - Use batch file:
  BUILD.bat

Option C - Manual compile:
  g++ -std=c++17 -Iinclude src/*.cpp -o HybridIDE.exe ^
      -lole32 -loleaut32 -luuid -mwindows

═══════════════════════════════════════════════════════════════

STEP 5: Verify HybridIDE.exe was created
─────────────────────────────────────────

dir HybridIDE.exe

You should see:
  HybridIDE.exe  (size: ~500 KB - 1 MB)

═══════════════════════════════════════════════════════════════

STEP 6: Run your IDE!
─────────────────────────────────────────

.\HybridIDE.exe

Or just double-click HybridIDE.exe in Explorer

═══════════════════════════════════════════════════════════════
                        WHAT HAPPENS
═══════════════════════════════════════════════════════════════

When you run HybridIDE.exe:

1. Splash screen appears (animated)
2. IDE loads (2-3 seconds)
3. Main window opens:
   ├── File Explorer (left)
   ├── Code Editor (center)
   ├── Toolbar (top)
   └── Status Bar (bottom)

═══════════════════════════════════════════════════════════════
                    TROUBLESHOOTING
═══════════════════════════════════════════════════════════════

ERROR: "g++ not found"
FIX:
  1. Install MinGW from https://www.msys2.org/
  2. Add C:\mingw64\bin to PATH
  3. Restart terminal
  4. Test: g++ --version

─────────────────────────────────────────

ERROR: "npm not found"
FIX:
  1. Install Node.js from https://nodejs.org/
  2. Restart terminal
  3. Test: npm --version

─────────────────────────────────────────

ERROR: Build fails
FIX:
  1. Delete node_modules folder in frontend/
  2. Delete dist folder in frontend/
  3. Run npm install again
  4. Run npm run build again
  5. Retry compilation

─────────────────────────────────────────

ERROR: EXE doesn't run
FIX:
  1. Install WebView2 Runtime
  2. Update Windows 11
  3. Check antivirus isn't blocking

═══════════════════════════════════════════════════════════════
                    COMPLETE COMMAND
═══════════════════════════════════════════════════════════════

Copy and paste this entire command:

cd frontend && npm install && npm run build && cd .. && g++ -std=c++17 -Iinclude src/main.cpp src/webview_host.cpp src/compiler.cpp src/system_info.cpp src/prompt_verifier.cpp -o HybridIDE.exe -lole32 -loleaut32 -luuid -lshell32 -mwindows && echo "SUCCESS! Run HybridIDE.exe"

═══════════════════════════════════════════════════════════════
                      FILE STRUCTURE
═══════════════════════════════════════════════════════════════

After building:

hybrid-ide/
├── HybridIDE.exe          ← THIS IS YOUR APP!
├── frontend/
│   ├── dist/              ← React UI (used by .exe)
│   │   ├── index.html
│   │   └── assets/
│   ├── node_modules/      ← Dependencies
│   └── src/               ← Source code
├── src/                   ← C++ source
├── include/               ← C++ headers
└── [documentation files]

═══════════════════════════════════════════════════════════════
                    DISTRIBUTION
═══════════════════════════════════════════════════════════════

To share your IDE:

1. Copy HybridIDE.exe
2. Copy frontend/dist/ folder (rename to webview/)
3. Zip together
4. Share!

Users only need:
  - Windows 11
  - WebView2 Runtime (usually pre-installed)
  - MinGW (to compile their C++ code)

═══════════════════════════════════════════════════════════════
                      QUICK TIPS
═══════════════════════════════════════════════════════════════

✓ Frontend only needs rebuilding if you change UI
✓ Backend rebuilds quickly (< 30 seconds)
✓ Keep both .exe and dist/ folder together
✓ The .exe is portable - no installation needed
✓ Works on any Windows 11 machine

═══════════════════════════════════════════════════════════════
                    YOU'RE DONE!
═══════════════════════════════════════════════════════════════

                     HybridIDE.exe
                          ↓
                   Double-click to run
                          ↓
                  Start coding in C++!

═══════════════════════════════════════════════════════════════

Questions? Check:
  - QUICKBUILD.md
  - WINDOWS_BUILD_GUIDE.md
  - README.md

═══════════════════════════════════════════════════════════════
